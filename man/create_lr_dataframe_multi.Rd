% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multisample.R
\name{create_lr_dataframe_multi}
\alias{create_lr_dataframe_multi}
\title{Create ligand–receptor score table across multiple datasets}
\usage{
create_lr_dataframe_multi(
  lr_scores_list,
  lrpairs,
  sample_groups = NULL,
  complex_sep = ", "
)
}
\arguments{
\item{lr_scores_list}{A list containing \code{res_list} (LR score matrices),
\code{ligand_scores_list} (ligand score matrices), and \code{receptor_scores_list}
(receptor score matrices). Each must be a named list with matching names.}

\item{lrpairs}{A data frame with columns \code{ligand} and \code{receptor} where
row names are the ligand–receptor interaction IDs used to join to scores.}

\item{sample_groups}{Optional named character vector mapping dataset names
(names of \code{lr_scores_list$res_list}) to a condition label. If \code{NULL},
\code{condition} is set to \code{NA}.}

\item{complex_sep}{Character separator used for ligand/receptor complexes, passed
to \code{create_lr_dataframe}.}
}
\value{
A data frame combining all per-dataset outputs of \code{create_lr_dataframe},
with two additional columns: \code{dataset} (dataset name) and \code{condition}
(mapped from \code{sample_groups}, or \code{NA}).
}
\description{
Applies \code{create_lr_dataframe} to each dataset in a score-list object, then
combines results into one data frame and adds dataset-level metadata.
}
