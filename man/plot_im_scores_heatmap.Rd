% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_im_scores_heatmap}
\alias{plot_im_scores_heatmap}
\title{plot_im_scores_heatmap}
\usage{
plot_im_scores_heatmap(
  df,
  interactions = NULL,
  geneCol = "Gene",
  geneCutOff = 20,
  out = "ScoresHeatmap.png",
  fontsize_row = 7,
  fontsize_col = 7,
  cluster_cols = TRUE,
  cluster_rows = TRUE,
  ...
)
}
\arguments{
\item{df}{A data.frame with genes as rownames and IMscores as columns. If
\code{geneCol} is not \code{NULL}, that column is used as gene names.}

\item{interactions}{A character vector of column names to subset the
heatmap to specific interactions. Default: \code{NULL} (use all columns).}

\item{geneCol}{Character specifying the column with the gene names.
Default: \code{"Gene"}. If \code{NULL}, assumes rownames already hold
gene names.}

\item{geneCutOff}{Integer. Number of genes (rows) to plot, based on
descending maximum score per gene. Default: \code{20}.}

\item{out}{Optional filename for saving the heatmap directly via
\code{pheatmap}. Default: \code{"ScoresHeatmap.png"}. Set to \code{NULL}
to avoid automatic saving.}

\item{fontsize_row}{Numeric. Size of row text. Default: \code{7}.}

\item{fontsize_col}{Numeric. Size of column text. Default: \code{7}.}

\item{cluster_cols}{Logical. Whether to cluster columns. Default: \code{TRUE}.}

\item{cluster_rows}{Logical. Whether to cluster rows. Default: \code{TRUE}.}

\item{...}{Additional parameters passed to \code{pheatmap::pheatmap()}.}
}
\value{
A \code{pheatmap} object (invisibly). To draw it, use \code{print(ph)}.
}
\description{
A pheatmap wrapper to plot the top IMscores based on the maximum score
per row, where the number of rows is defined by \code{geneCutOff}.
}
\examples{
\dontrun{
# Basic usage with automatic saving (PNG)
ph <- plot_scores_heatmap(imScores, geneCutOff = 15)

# No automatic file, then save a custom TIFF via tiff()/dev.off():
ph <- plot_scores_heatmap(imScores, geneCutOff = 15, out = NULL)
tiff("IMscoresHeatmap.tiff", width = 7, height = 7, units = "in", res = 300)
print(ph)
dev.off()
}

}
