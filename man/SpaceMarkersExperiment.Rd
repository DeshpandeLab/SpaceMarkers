% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R
\name{SpaceMarkersExperiment}
\alias{SpaceMarkersExperiment}
\title{Create a SpaceMarkersExperiment object}
\usage{
SpaceMarkersExperiment(
  assays,
  colData = NULL,
  spatialCoords = NULL,
  rowData = NULL,
  spaceMarkers = S4Vectors::SimpleList(),
  features = NULL,
  ...
)
}
\arguments{
\item{assays}{A \code{SpatialExperiment} object to coerce, or a list of
assay matrices (e.g., logcounts) when building from scratch.}

\item{colData}{A DataFrame of per-spot metadata. Ignored when \code{assays}
is a SpatialExperiment.}

\item{spatialCoords}{A numeric matrix of spatial coordinates (spots x 2).
Ignored when \code{assays} is a SpatialExperiment.}

\item{rowData}{Optional DataFrame of per-gene metadata. Ignored when
\code{assays} is a SpatialExperiment.}

\item{spaceMarkers}{A SimpleList or list for SpaceMarkers results.
Defaults to an empty SimpleList.}

\item{features}{Optional: a file path, matrix/data.frame, or R object
(e.g., CoGAPS result) of spatial features to add via
\code{\link{add_features}}.}

\item{...}{Additional arguments passed to \code{SpatialExperiment()} when
building from scratch, or to \code{\link{get_spatial_features}} when
\code{features} is provided.}
}
\value{
A \code{SpaceMarkersExperiment} object.
}
\description{
Constructor for the SpaceMarkersExperiment class. Accepts
either a \code{SpatialExperiment} object to coerce or raw components
(assays, coordinates, etc.) to build from scratch. When an SPE is
provided, features can optionally be added in the same call via
\code{\link{add_features}}.
}
