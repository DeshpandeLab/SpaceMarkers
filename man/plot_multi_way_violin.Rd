% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_multi_way_violin}
\alias{plot_multi_way_violin}
\title{Plot multi-way violin(s) for SpaceMarkers pattern pairs (directed / undirected / both)}
\usage{
plot_multi_way_violin(
  expr_hotspots,
  patternpair,
  gene,
  cell_id = "barcode",
  mode = c("directed", "undirected", "both"),
  remove_na = TRUE,
  colors = c("blue", "orange", "red", "purple", "darkgreen"),
  out = NULL,
  width = 10,
  height = 10,
  text_size = 14
)
}
\arguments{
\item{expr_hotspots}{\code{data.frame} like \code{test_hotspot_expr} produced by your
SpaceMarkers helpers. Must contain:
\itemize{
\item a cell/spot id column named by \code{cell_id} (default \code{"barcode"})
\item a numeric expression column for the target \code{gene} (the column name is the
gene symbol you pass in \code{gene})
\item zero or more of the SpaceMarkers region columns:
\code{"<cell1>_<cell2>"}, \code{"<cell1>_near_<cell2>"}, \code{"<cell2>_near_<cell1>"}
}}

\item{patternpair}{\code{character(2)}. The pair \code{c(cell1, cell2)}, e.g.,
\code{c("Epi","Plasma")}. Used to derive the expected column names.}

\item{gene}{\code{character(1)}. The \strong{column name} in \code{expr_hotspots} that holds
the expression values to plot (typically a gene symbol).}

\item{cell_id}{\code{character(1)}. Name of the cell/spot id column. Default \code{"barcode"}.}

\item{mode}{\code{character(1)}. One of \code{c("directed","undirected","both")}.
\itemize{
\item \code{"directed"} → uses \code{"<cell1>_near_<cell2>"} and \code{"<cell2>_near_<cell1>"}
and enforces order \code{c(cell1, cell1_near_cell2, cell2_near_cell1, cell2)}
\strong{if} all groups are present; otherwise plots as-is.
\item \code{"undirected"} → uses only \code{"<cell1>_<cell2>"} with values
\code{cell1}/\code{"Interacting"}/\code{cell2}, enforcing order
\code{c(cell1, "Interacting", cell2)} if present; else as-is.
\item \code{"both"} → directed groups plus \strong{only} undirected \code{"Interacting"}
(renamed to \code{"<cell1>_<cell2>"}), enforcing order
\code{c(cell1, cell1_near_cell2, cell1_cell2, cell2_near_cell1, cell2)} if present.
}}

\item{remove_na}{\code{logical(1)}. If \code{TRUE} (default), drop rows with \code{NA} in \code{region}.}

\item{colors}{\code{character}. Fill colors. If enforcing a mode’s order, the first
\code{length(desired_order)} colors are used; otherwise colors are matched to the
present groups. Recycled if shorter than needed.}

\item{out}{\code{character(1)} or \code{NULL}. File path to save the plot via \code{ggsave}.
Use \code{NULL} to skip saving. Default \code{NULL}.}

\item{width, height}{\code{numeric(1)}. Plot size in inches when saving. Defaults \code{10}.}

\item{text_size}{Numeric base size for theme text. Default: 14.}
}
\value{
A \code{ggplot} object (invisibly). Optionally writes an image if \code{out} is not \code{NULL}.
}
\description{
Generate violin + jitter plots of a single gene's expression across
SpaceMarkers-derived region labels for a given pattern pair.
This function \strong{does not} rely on base membership columns; it only uses:
\itemize{
\item \code{"<cell1>_<cell2>"} (undirected, e.g., \code{"Epi_Plasma"})
\item \code{"<cell1>_near_<cell2>"} (directed, e.g., \code{"Epi_near_Plasma"})
\item \code{"<cell2>_near_<cell1>"} (directed, e.g., \code{"Plasma_near_Epi"})
}

For \strong{directed} columns, any literal value \code{"Interacting"} is replaced with
the column name (e.g., \code{"Epi_near_Plasma"}).
For \strong{undirected}:
\itemize{
\item \code{mode = "undirected"} keeps \code{"Interacting"} as is and orders
\code{c(cell1, "Interacting", cell2)} if all groups are present.
\item \code{mode = "both"} keeps \strong{only} the \code{"Interacting"} rows and renames them
to \code{"<cell1>_<cell2>"} before binding with the directed groups.
}

If the expected categories for a mode are \strong{not all present} (or there are
fewer than 3 total groups), the function falls back to plotting the present
groups “as is” (first-appearance order).
}
\examples{
\dontrun{
# --- Minimal reproducible example setup ---
set.seed(123)

# Expression matrix (spCounts)
spCounts <- data.frame(
  barcode = paste0("Spot", 1:10),
  SAMD11  = runif(10, 0, 5),
  NOC2L   = runif(10, 0, 5)
)

# Base hotspots (spHotspots)
spHotspots <- data.frame(
  barcode    = spCounts$barcode,
  Epi_Plasma = sample(c("Epi", "Interacting", "Plasma"), 10, TRUE)
)

# Directed influence hotspots (spHotspots_influence)
spHotspots_influence <- data.frame(
  barcode          = spCounts$barcode,
  Epi_near_Plasma  = sample(c("Epi", "Interacting", NA), 10, TRUE),
  Plasma_near_Epi  = sample(c("Plasma", "Interacting", NA), 10, TRUE)
)

# --- SpaceMarkers-style workflow ---
interacting_hotspots <- classify_spots(
  pat_hotspots       = spHotspots,
  influence_hotspots = NULL,
  patternpair        = c("Epi","Plasma")
)

spHotspots_all <- cbind(spHotspots, interacting_hotspots)
spHotspots_all[["Epi_Plasma"]] <- factor(
  spHotspots_all[["Epi_Plasma"]],
  levels = c("Epi","Interacting","Plasma")
)

test_hotspots <- classify_spots(
  pat_hotspots       = spHotspots_all,
  influence_hotspots = spHotspots_influence,
  patternpair        = c("Epi","Plasma")
)

multiway_test_hotspots <- cbind(spHotspots_all, test_hotspots)
test_hotspot_expr <- get_hotspot_expr(multiway_test_hotspots, spCounts)

# --- Demonstration using mode = "both" ---
p <- plot_multi_way_violin(
  expr_hotspots = test_hotspot_expr,
  patternpair   = c("Epi","Plasma"),
  gene          = "SAMD11",         # any gene column in spCounts
  mode          = "both",
  remove_na     = TRUE,
  colors        = c("blue","yellow","green","red","orange"),
  out           = NULL              # or specify a filename
)
print(p)
}
}
