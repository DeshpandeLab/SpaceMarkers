% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_spatial}
\alias{plot_spatial}
\title{plot_spatial}
\usage{
plot_spatial(
  sme,
  feature_col = NULL,
  hotspot_type = c("undirected", "pattern", "influence"),
  source = c("auto", "colData", "assay", "hotspots", "influence_map", "interaction"),
  interaction_patterns = NULL,
  interaction_label = NULL,
  image = NULL,
  resolution = c("lowres", "hires", "fullres"),
  colors = NULL,
  point_size = 2.5,
  stroke = 0.05,
  alpha = 0.5,
  title = feature_col,
  bg_color = NULL,
  crop = TRUE,
  text_size = 15
)
}
\arguments{
\item{sme}{A \code{SpaceMarkersExperiment} object.}

\item{feature_col}{Character. Name of the feature to plot. Searched in
order: \code{colData(sme)}, \code{rownames(sme)} (gene expression),
hotspots metadata for the specified \code{hotspot_type}.}

\item{hotspot_type}{One of "undirected", "pattern", "influence". Only
consulted if \code{feature_col} is not found in colData or rownames.}

\item{image}{Optional: a raster or matrix image to overlay. If NULL,
tries \code{imgRaster(sme)}, then the stored \code{visiumDir} from
\code{load10X()}, then falls back to no image.}

\item{resolution}{Image resolution used when loading from
\code{visiumDir}. Default "lowres".}

\item{point_size, stroke, alpha, title, bg_color, text_size, colors}{ggplot
aesthetics (see \code{plot_spatial_data_over_image}).}

\item{crop}{Logical; crop the view to the spot bounding box. Default TRUE.}
}
\value{
A ggplot object.
}
\description{
SME-native spatial plot. Extracts coordinates from the
\code{SpaceMarkersExperiment}, looks up \code{feature_col} in colData,
rownames (gene expression), or hotspot metadata, and overlays on the
tissue image if available. If no image is found, plots on a blank
background.
}
