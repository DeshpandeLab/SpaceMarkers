% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multisample.R
\name{plot_lr_scores_scatter}
\alias{plot_lr_scores_scatter}
\title{Plot ligand receptor scores by interaction}
\usage{
plot_lr_scores_scatter(
  df_long,
  out_dir,
  filename_prefix = "lr_scatter",
  source_to_target = NULL,
  point_size = 2,
  point_alpha = 0.8,
  width = 8,
  height = 6,
  dpi = 300
)
}
\arguments{
\item{df_long}{Long-format data.frame, usually from get_all_lr_interactions,
containing ligand_score, receptor_score, interaction, sample, and
source_to_target columns.}

\item{out_dir}{Character. Output directory for TIFF files. Created if needed.}

\item{filename_prefix}{Character. Prefix for output file names.}

\item{source_to_target}{Character vector of source_to_target values to plot.
If NULL, all unique values are used.}

\item{point_size}{Numeric. Point size for geom_point.}

\item{point_alpha}{Numeric between 0 and 1. Alpha transparency for points.}

\item{width}{Numeric. Plot width in inches.}

\item{height}{Numeric. Plot height in inches.}

\item{dpi}{Numeric. Resolution in dots per inch for the saved TIFF files.}
}
\value{
Invisibly, a character vector of paths to the saved TIFF files.
}
\description{
Make scatter plots of ligand score versus receptor score, colored by
interaction and shaped by sample, and save one TIFF file per
source_to_target combination.
}
\examples{
\dontrun{
if (requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("RColorBrewer", quietly = TRUE) &&
    requireNamespace("circlize", quietly = TRUE)) {

  set.seed(123)
  df_long <- data.frame(
    ligand_score    = runif(50, 0, 1),
    receptor_score  = runif(50, 0, 1),
    interaction     = sample(c("LGF1_REC1", "LGF2_REC2", "LGF3_REC3"), 50, TRUE),
    sample          = sample(c("NS_1", "NS_2", "VU_1"), 50, TRUE),
    source_to_target = sample(c("Epi_to_Tcell", "Fibro_to_Myeloid"), 50, TRUE),
    stringsAsFactors = FALSE
  )

  out_dir <- tempdir()

  # Plot all source_to_target combinations
  files_all <- plot_lr_scores_scatter(
    df_long        = df_long,
    out_dir        = out_dir,
    filename_prefix = "lr_scatter",
    source_to_target = NULL,
    point_size      = 2,
    point_alpha     = 0.8
  )

  # Plot only one specific source_to_target
  files_one <- plot_lr_scores_scatter(
    df_long        = df_long,
    out_dir        = out_dir,
    filename_prefix = "Epi_Tcell_lr_scatter",
    source_to_target = "Epi_to_Tcell"
  )
}
}

}
