% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotMultiWayViolin.R
\name{mergeAllHotspotDfs}
\alias{mergeAllHotspotDfs}
\title{Merge Pattern/Influence Tables Across Many Features}
\usage{
mergeAllHotspotDfs(
  pattern_hotspots,
  influence_hotspots,
  features = NULL,
  cell_id = "barcode",
  influence_suffix = "-influence"
)
}
\arguments{
\item{pattern_hotspots}{\code{data.frame} with \code{cell_id} and feature columns.}

\item{influence_hotspots}{\code{data.frame} with \code{cell_id} and matching features.}

\item{features}{Character vector of features to merge. If \code{NULL}, the
intersection of non-\code{cell_id} column names is used.}

\item{cell_id}{Common identifier column (default \code{"barcode"}).}

\item{influence_suffix}{Suffix for influence columns (default \code{"-influence"}).}
}
\value{
A wide \code{data.frame} with one row per \code{cell_id} and, for each feature
\code{f}, two columns: \code{f} (pattern) and \code{paste0(f, influence_suffix)}.
}
\description{
Iterates over \code{features}, calling \code{\link[=mergeHotspotDfs]{mergeHotspotDfs()}} for each, and returns a
single wide table containing pattern and influence columns per feature,
aligned by \code{cell_id}.
}
\examples{
p <- data.frame(barcode = c("a","b"),
                A = c(1, NA),
                B = c(NA, 2))
i <- data.frame(barcode = c("a","b"),
                A = c(NA, "a"),
                B = c("b", NA))
mergeAllHotspotDfs(p, i, features = c("A","B"))
}
\seealso{
\code{\link[=mergeHotspotDfs]{mergeHotspotDfs()}}
}
