% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotMultiWayViolin.R
\name{addInteractCol}
\alias{addInteractCol}
\title{Add an Interaction Label Column for a Pair of Hotspot Features}
\usage{
addInteractCol(
  hotspots,
  interaction_cols = c("refPattern", "OtherPattern"),
  interact_name = "Interacting",
  interaction_col_name = "interaction"
)
}
\arguments{
\item{hotspots}{A \code{data.frame} with at least two columns that
encode hotspot membership as \code{NA}/non-\code{NA}.}

\item{interaction_cols}{Character vector of length 2 giving the column names
to compare (e.g., \code{c("refPattern", "OtherPattern")}). The first name is
treated as the reference (\code{ref}), the second as the \code{other}.}

\item{interact_name}{Label to assign when both features are present
in the same row. Default \code{"Interacting"}.}

\item{interaction_col_name}{Name of the new column to create.}
}
\value{
\code{hotspots} with one additional factor column \code{interaction_col_name}
whose levels are \verb{c(<ref>, <interact_name>, <other>)}.
}
\description{
Creates a factor label per row indicating whether neither, one, or both
hotspot features are present (non-\code{NA}). The new factor has levels in the
order: \verb{<ref>}, \verb{<interact_name>}, \verb{<other>}.
}
\details{
A row is labeled:
\itemize{
\item \verb{<interact_name>} if both columns are non-\code{NA}
\item \verb{<ref>} if only the first column is non-\code{NA}
\item \verb{<other>} if only the second column is non-\code{NA}
\item \code{NA} if both are \code{NA}
}

A warning is emitted if all labels are \code{NA}.
}
\examples{
tmp <- data.frame(refPattern = c("a", NA, NA),
                  OtherPattern = c("b", "b", NA))
tmp2 <- addInteractCol(tmp,
  interaction_cols = c("refPattern", "OtherPattern"),
  interaction_col_name = "ref_vs_other"
)
print(tmp2)
}
\seealso{
\code{\link[=addPairwiseInteractCol]{addPairwiseInteractCol()}}, \code{\link[=plotMultiWayViolin]{plotMultiWayViolin()}}
}
