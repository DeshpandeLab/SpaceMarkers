% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotMultiWayViolin.R
\name{addPairwiseInteractCol}
\alias{addPairwiseInteractCol}
\title{Add All Pairwise Interaction Columns Across Features}
\usage{
addPairwiseInteractCol(
  hotspots,
  features = NULL,
  interact_name = "Interacting"
)
}
\arguments{
\item{hotspots}{A \code{data.frame} with hotspot feature columns and (optionally)
an identifier column named \code{"barcode"}.}

\item{features}{Character vector of feature column names to pair. If \code{NULL},
all columns except \code{"barcode"} are used.}

\item{interact_name}{Label to assign when both features are present in the
same row Default \code{"Interacting"}}
}
\value{
The input \code{hotspots} with one extra factor column per pair named
\code{"A_B"}. Duplicate columns (by name) are deduplicated.
}
\description{
For a set of hotspot feature columns, compute an interaction label
(see \code{\link[=addInteractCol]{addInteractCol()}}) for every unique ordered pair \code{A_B}. Each resulting
column is a factor with levels \code{c("A", "Interacting", "B")}.
}
\examples{
df <- data.frame(barcode = paste0("s", 1:3),
                A = c("a", NA, "a"),
                B = c(NA, "B", "B"),
                C = c(NA, NA,"C"))
out <- addPairwiseInteractCol(df, features = c("A", "B", "C"))
print(out)
}
\seealso{
\code{\link[=addInteractCol]{addInteractCol()}}, \code{\link[=plotMultiWayViolin]{plotMultiWayViolin()}}
}
